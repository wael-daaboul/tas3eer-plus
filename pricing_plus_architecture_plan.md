# خطة التطوير المعمارية الشاملة لمنصة "تسعير+ / Pricing+"

## 1. ملخص تنفيذي قصير
مشروع "تسعير+" بصيغته الحالية هو أداة تعمل من جهة العميل (Client-side) لتقديم حسابات تسعير مرنة ومفيدة. رغم نجاح أداة الـ MVP الحالية، إلا أن الاعتماد الكلي على Vanilla JavaScript وملف `main.js` ضخم (أكثر من 2700 سطر) يمثل عنق زجاجة تقني يمنع التوسع. لتحويل "تسعير+" إلى منصة متكاملة (تحتوي على مدونة ذات تواجد قوي في محركات البحث SEO، ونظام تعليقات، ولوحة تحكم للإدارة)، يجب الانتقال من بنية (Vanilla HTML/JS/CSS) إلى إطار عمل حديث يدعم العرض من جهة الخادم (SSR) وبناء واجهات برمجة التطبيقات (API Routes). 

## 2. تشخيص المشروع الحالي

### نقاط الضعف التقنية الحالية:
- **تضخم `main.js`**: الملف يحتوي على حالة التطبيق (State)، ربط أحداث واجهة المستخدم (DOM Events)، والمنطق التجاري (Business Logic)، مما يجعل صيانته شبه مستحيلة مستقبلاً.
- **التوجيه (Routing)**: غياب نظام توجيه حقيقي، مما يجعل إنشاء صفحات للمقالات (Blog) ولوحة الإدارة عملية معقدة وغير قابلة للأرشفة (SEO).
- **أمان الواجهة الخلفية**: النظام الحالي يعتمد على IndexedDB مع مزامنة اختيارية لـ Supabase للنسخ الاحتياطي (Backup). لتوفير مقالات وتعليقات، نحتاج إلى قاعدة بيانات مركزية مع صلاحيات أمان (RLS).
- **إدارة الحالة (State Management)**: تتم عبر كائن `state` عام يتم تعديله مباشرة، بدون تفاعلية (Reactivity) حقيقية.

### ما يجب الحفاظ عليه:
- **منطق التسعير (`pricingEngine.js`)**: يمكن إعادة استخدامه بالكامل لأنه مكتوب كـ Pure Functions لا تعتمد على الـ DOM.
- **منطق التصدير (`exportService.js`)**: مفيد وقابل للنقل.
- **المظهر العام (CSS)**: يمكن تفكيك `styles.css` إلى مكونات (Components) أو الانتقال لإطار عمل مثل Tailwind CSS للحفاظ على الرؤية الفنية مع مرونة أكبر.

### تقييم المشاكل:
- **مشاكل مؤكدة من الكود**: صعوبة الصيانة وإضافة الميزات بسبب تداخل DOM مع الـ Logic. عدم توافق البنية الحالية مع متطلبات الـ SEO للمدونة.
- **مخاطر محتملة**: فقدان بيانات المستخدمين المحليين إذا تم مسح بيانات المتصفح (IndexedDB) قبل القيام بنسخ احتياطي. حقن سكربتات (XSS) في التعليقات إذا أُضيفت بالبنية الحالية.
- **تحسينات مستقبلية**: فصل التطبيق إلى واجهة مستخدم مبسطة والتخلص من التحديث اليدوي للـ DOM.

## 3. القرار المعماري المقترح

**الخيار الأمثل: الانتقال إلى Next.js أو Nuxt 3.**
نظراً للحاجة الماسة إلى (SEO للمدونة، API للتعليقات، ولوحة تحكم)، أوصي بالانتقال إلى **Next.js (App Router)** أو **Nuxt 3**.
*سبب الاختيار (بافتراض مطور واحد أو فريق صغير):*
- **SEO & SSR**: يدعمان Server-Side Rendering بشكل ممتاز، وهو متطلب حتمي لأرشفة المقالات.
- **APIs مدمجة**: يتيحان كتابة الـ Backend والـ Frontend في مشروع واحد (مثالي للتعليقات وتوثيق الـ Captcha).
- **بيئة Supabase**: التكامل بين Next.js/Nuxt و Supabase مدعوم رسمياً بأدوات قوية جداً من حيث (Auth و الداتابيز و RLS).

**القرار**: سنعتمد **Next.js (App Router)** مع **Supabase** و **Tailwind CSS**. (أو Nuxt 3 إذا كان المطور يفضل Vue، الهيكلية ستكون مشابهة جداً).

## 4. البنية الجديدة (هيكل المجلدات)

هيكل المجلدات المقترح لمشروع Next.js:

```text
تسعير+/
├── src/
│   ├── app/                    # صفحات الواجهة (App Router)
│   │   ├── (public)/           # الصفحات العامة (الرئيسية، من نحن...)
│   │   ├── (app)/              # أداة التسعير نفسها (التطبيق)
│   │   ├── blog/               # نظام المقالات (المدونة)
│   │   ├── admin/              # لوحة تحكم الإدارة
│   │   └── api/                # واجهات برمجة التطبيقات (API routes)
│   ├── components/
│   │   ├── ui/                 # مكونات عامة (أزرار، حقول إدخال)
│   │   ├── calculator/         # مكونات أداة التسعير (الآلة الحاسبة)
│   │   ├── blog/               # مكونات المدونة (محرر، بطاقة مقال)
│   │   └── admin/              # مكونات خاصة بلوحة الإدارة
│   ├── lib/
│   │   ├── engine/             # (نقل pricingEngine.js هنا)
│   │   ├── supabase/           # إعدادات وعميل Supabase
│   │   └── utils/              # دوال المساعدة (id.js, format.js)
│   ├── stores/                 # إدارة الحالة (Zustand أو Vuex/Pinia لو استخدمنا Nuxt)
│   ├── i18n/                   # ملفات الترجمة والتدويل (ar, en)
│   └── types/                  # تعريفات TypeScript (مستحسن بشدة)
```

## 5. قاعدة البيانات المقترحة (Supabase PostgreSQL)

البنية الأساسية المضافة لدعم الميزات الجديدة:
- **`projects` / `users`**: المستخدمون وحساباتهم.
- **`posts`**: `id`, `slug`, `title`, `content`, `excerpt`, `cover_image`, `status` (draft/published), `seo_title`, `seo_description`, `created_at`, `category_id`.
- **`categories`**: `id`, `name`, `slug`.
- **`comments`**: `id`, `post_id`, `author_name` (optional), `author_email` (optional), `content`, `status` (pending/approved/spam), `ip_address` (للحد من السبام), `created_at`.
- **`settings`**: `key`, `value` (لإعدادات الموقع العامة من لوحة التحكم).

*سيتم تفعيل RLS (Row Level Security) في Supabase:*
- `posts` (المنشورة) يمكن للجميع قراءتها. الكتابة فقط للمدير (Admin).
- `comments` (المقبولة) يمكن للجميع قراءتها. الإضافة متاحة للعموم ولكن الحالة الافتراضية `pending`.

## 6. نظام التعليقات المقترح (تعليقات مجهولة آمنة)

- **البنية للتعليقات المجهولة**: إرسال طلب POST من العميل إلى `/api/comments`. القاعدة البيانات تخزن التعليق بحالة `pending`.
- **منع السبام (Spam)**: 
  - استخدام **Honeypot Field** (حقل مخفي في الواجهة إذا تم تعبئته من الـ Bots يتم رفض الطلب).
  - استخدام **Cloudflare Turnstile** أو **Google reCAPTCHA v3** (تتحقق في الـ backend API قبل الحفظ).
  - **Rate Limiting**: حد أقصى للتعليقات من نفس الـ IP كل 10 دقائق (يمكن إعداده عبر Supabase Edge Functions أو Vercel kv).
- **الموافقة**: التعليقات تحتاج لموافقة المشرف (لا تُنشر مباشرة للوقاية من الـ Spam/XSS).
- **تجنب XSS**: تعقيم المحتوى (Sanitization) في الـ Backend باستخدام مكتبة مثل `DOMPurify` (إذا كان يدعم HTML) أو والأفضل استخراج النصوص الجردة فقط (Plain Text).

## 7. نظام المقالات (المدونة)

- **الإنشاء/التعديل**: من لوحة الإدارة. سنستخدم محرر محتوى مثل **TipTap** أو **Quill** لتجربة كتابة احترافية (WYSIWYG).
- **الصور**: رفع الصور إلى Supabase Storage.
- **التوجيه والـ SEO**: استخدام Dynamic Routes مثل `/blog/[slug]`. توليد الـ Meta tags ديناميكياً بناءً على (عنوان SEO ووصف SEO) داخل الخادم לפני العرض.

## 8. نظام لوحة الإدارة المقترح (Admin Dashboard)

- **الوصول**: مجلد `/admin` محمي بـ Middleware يتأكد أن المستخدم الحالي هو مُدير (عبر Role في Supabase Users).
- **الميزات**:
  - صفحة إحصائيات (عدد المستخدمين الجدد، التعليقات قيد المراجعة).
  - إدارة المقالات (جدول عرض المقالات، رز تعديل، كتابة مقال جديد).
  - إدارة التعليقات (جدول بالتعليقات بحالة Pending مع أزرار Accept/Delete/Mark as Spam).
  - إدارة الأقسام (إنشاء فئات للمقالات).

## 9. خطة التنفيذ المرحلية (Roadmap)

### المرحلة 1: إصلاحات وتجهيز البيئة (الأسبوع 1)
- **الهدف**: تهيئة بيئة العمل الجديدة (Next.js/Nuxt) وإعداد Supabase.
- **المهام**: تهيئة المشروع، ربط قاعدة البيانات، نسخ `pricingEngine.js` والتأكد من عمل اختباراته المستقلة، بناء الـ Layout الأساسي.
- **المخاطر**: بطء في البداية بسبب تغير الهيكلية.
- **الأولوية**: عالية جداً (حرجة).

### المرحلة 2: إعادة هيكلة الأداة الحالية "تسعير+" (الأسبوع 2-3)
- **الهدف**: نقل أداة الـ MVP الحالية من `main.js` إلى المكونات (Components) الجديدة وإدارة الحالة (Zustand/Pinia).
- **المهام**: تفكيك واجهة المستخدم إلى مكونات (Settings, Materials, Products, Results). استبدال IndexedDB بـ State محلي ومزامنة مع IndexedDB/Supabase بأسلوب أحدث.
- **المخاطر**: تراجع الأداء إذا تم إعادة العرض (Re-renders) بكثرة في المكونات المعقدة.

### المرحلة 3: إضافة المدونة ونظام المقالات (الأسبوع 4)
- **الهدف**: بناء أقسام المدونة لعرض المحتوى بقوة SEO.
- **المهام**: إنشاء جداول `posts`، إنشاء مكونات الواجهة (Blog List, Article Page)، تطبيق الـ SSR والـ Meta tags.

### المرحلة 4: نظام لوحة الإدارة (الأسبوع 5)
- **الهدف**: بناء الـ Admin UI للتحكم بالمدونة.
- **المهام**: Middleware للمدراء، دمج محرر المحتوى TipTap، ربط الـ API للـ CRUD الخاصة بالمقالات.

### المرحلة 5: نظام التعليقات (الأسبوع 6)
- **الهدف**: تفعيل تفاعل المستخدمين עם المقالات بأمان.
- **المهام**: بناء وتركيب الـ UI للتعليقات، إعداد API endpoint لإنشاء تعليق، إضافة CAPTCHA و Honeypot، واجهة المراجعة في لوحة الإدارة.

### المرحلة 6: تحسينات SEO والأداء والتدويل (i18n) (الأسبوع 6-7)
- **الهدف**: الوصول لأفضل تجربة مستخدم وأداء بحث.
- **المهام**: تحسين الترجمة لتعتمد معماريات إطارات العمل (next-intl مثلاً)، ضغط الصور، ربط Sitemaps و robots.txt ديناميكياً.

### المرحلة 7: الاختبارات والإطلاق (الأسبوع 8)
- **الهدف**: تأكيد دقة عمل التطبيق وخلوه من الثغرات.
- **المهام**: كتابة اختبارات E2E للوظائف الأساسية بـ Cypress/Playwright، تجربة هجرة البيانات لقدامى المستخدمين، النشر (على Vercel أو الخادم الخاص).

## 10. قائمة أول 20 مهمة تطوير يجب البدء بها فوراً

1. إنشاء مخرجات (Repository) جديد لمشروع Next.js / Nuxt.
2. تثبيت المكتبات الأساسية (Tailwind CSS, Supabase JS Client).
3. إعداد هيكل المجلدات (`/components`, `/lib`, `/app`).
4. نقل وتغليف ملف `pricingEngine.js` إلى `lib/engine/pricingEngine.js`.
5. كتابة اختبارات الوحدات (Unit Tests) لـ `pricingEngine.js` للتأكد من عدم كسره.
6. إضافة TypeScript للمشروع (اختياري لكن مفضل جداً، على الأقل تعريف الانترفيس للمواد والمنتجات).
7. إعداد قواعد الجداول في Supabase (`users`, `posts`, `categories`, `comments`).
8. تفعيل RLS (Row Level Security) على جداول Supabase للسماح للقراءة فقط للمقالات.
9. إنشاء ملف نظام الملاحة (Navbar) والـ Footer كـ Shared Components.
10. بناء شاشة (الآلة الحاسبة) وتحويل `stepSettings` إلى مكون `ProjectSettings.tsx`.
11. تحويل `stepMaterials` إلى مكون `MaterialsLibrary.tsx`.
12. تحويل `stepProducts` إلى مكون `ProductsList.tsx`.
13. تحويل `stepResults` إلى مكون `CalculationResults.tsx`.
14. إعداد مكتبة إدارة الحالة (Global State) مثل Zustand لتخزين المشروع والمواد والمنتجات بدلاً من Global Const State.
15. تنفيذ آلية تزامن الحالة المحلية مع IndexedDB ليعمل التطبيق Offline.
16. إنشاء Layout رئيسي (AppLayout و BlogLayout لكلا القسمين).
17. إعداد التوجيه للمدونة `/blog/page.tsx` و `/blog/[slug]/page.tsx`.
18. إعداد API Endpoint للتعليقات `/api/comments` يحتوي على Honeypot Validation.
19. إعداد Middleware الـ Auth للتمييز بين الـ (Admin) والمستخدم العادي وحماية مجلد `/admin`.
20. بناء صفحة لوحة الإدارة الأساسية `/admin/page.tsx` لقراءة البيانات الإحصائية الأساسية.
