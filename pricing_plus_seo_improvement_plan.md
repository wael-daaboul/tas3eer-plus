# خطة تحسين محركات البحث (SEO) الشاملة لموقع "تسعير+" (Pricing+)

تعتمد هذه الخطة على تحليل الملفات الحالية للموقع (`index.html`, `sitemap.xml`, `robots.txt`)، وتهدف إلى جعل الموقع صديقاً ومقروءاً بشكل واضح لمحركات البحث (مثل Google)، مما يزيد من ظهوره للجمهور المستهدف (أصحاب المشاريع الصغيرة).

---

## 1. الأخطاء الحرجة المطلق إصلاحها فوراً (Critical Fixes)

من خلال مراجعة الملفات، تبين وجود بعض الأخطاء التي تمنع محركات البحث من أرشفة الموقع بشكل صحيح:

*   **خريطة الموقع `sitemap.xml` غير صالحة:** الخريطة الحالية تستخدم مسارات نسبية (Relative Paths) مثل `<loc>/app/</loc>`. **يجب** أن تستخدم الروابط المطلقة (Absolute URLs)، وإلا سيتم رفضها من قبل Google Search Console.
    *   *الحل المقترح:* التعديل لتكون `<loc>https://pricingplus.app/app/</loc>` لجميع الروابط.
*   **ملف `robots.txt`:** يجب تحديث رابط الـ Sitemap ليكون رابطاً مطلقاً أيضاً:
    *   *الحل المقترح:* استبدال `Sitemap: /sitemap.xml` بـ `Sitemap: https://pricingplus.app/sitemap.xml`.

---

## 2. تحدي تعدد اللغات (Bilingual SEO)

الموقع الحالي يعتمد على الجافاسكريبت (`site.js`) لتبديل النصوص بين العربية والإنجليزية (Client-side translation) في نفس الصفحة (SPA style). 
**تأثير ذلك على الـ SEO:** عناكب محركات البحث غالباً ما تقرأ الكود المصدري الأولي (HTML)، مما يعني أنها ستؤرشف لغة واحدة فقط (التي تظهر في بداية تحميل الصفحة)، ولن تتعرف بشكل جيد على اللغة الأخرى.

### الحلول المقترحة (اختر أحدهما):
*   **الخيار الأفضل (طويل المدى):** فصل الروابط لكل لغة. على سبيل المثال مجلد `/en/` للإنجليزية والمجلد الرئيسي `/` للعربية. 
*   **الخيار المتقدم:** إذا كنت ترغب بالاحتفاظ بآلية عمل SPA، يمكنك استخدام وسوم `hreflang` بالتزامن مع توجيه الروابط، لكنه يتطلب إعدادات معقدة في السيرفر أو استخدام إطار عمل مثل Next.js / Nuxt.
*   **إضافة وسوم Hreflang:** ضرورية جداً لإخبار جوجل بوجود نسختين من الصفحة لتجنب تضارب المحتوى.

---

## 3. تحسينات الأكواد الوصفية والعلامات (Meta Tags & Head Optimization)

1.  **توحيد لغة الوسوم الأساسية (Meta Title & Description):**
    *   حالياً الوسم `<title>` بالعربية، بينما `meta name="title"` بالإنجليزية. يجب جعلها متطابقة مع لغة الصفحة (إذا كانت الصفحة عربي، يجب أن يكون الوصف وعنوان الـ OG بالعربي بالكامل).
2.  **إضافة وسم الـ Canonical (`rel="canonical"`):**
    *   يجب إضافته في قسم `<head>` لحماية الموقع من مشكلة المحتوى المكرر (Duplicate Content)، ليحدد الرابط الرسمي للصفحة:
    *   `<link rel="canonical" href="https://pricingplus.app/" />`
3.  **تحسين وصف الصفحة (Meta Description):**
    *   الوصف الحالي جيد، لكن يمكن تطعيمه بكلمات مفتاحية أكثر بحثاً باللغة العربية.
    *   *مثال:* "أداة تسعير+ المجانية تساعد المشاريع الصغيرة على حساب تكلفة المنتج، السعر الحقيقي، ونقطة التعادل بدقة. ابدأ حساب أرباحك الآن بدون تسجيل."

---

## 4. تحسين بنية المحتوى ومصادر الزيارات (Content Strategy & On-Page SEO)

1.  **الكلمات المفتاحية الاستراتيجية:** يجب توزيع الكلمات التالية بذكاء داخل نصوص الموقع (دون حشو):
    *   *حساب تكلفة المنتج*
    *   *كيفية تسعير المنتجات*
    *   *نقطة التعادل للمشاريع*
    *   *تسعير المشاريع الصغيرة*
    *   *حساب الأرباح*
2.  **هيكلة العناوين (Headings H1, H2, H3):**
    *   تأكد من وجود وسم `<h1>` واحد فقط في كل صفحة ويحتوي على الكلمة المفتاحية الأهم (الوضع الحالي في الرئيسية جيد).
    *   استخدم `<h2>` للفقرات الفرعية بطريقة تعكس أسئلة المستخدمين، مثل "كيف نحسب السعر الحقيقي؟".
3.  **النصوص البديلة للصور (Alt Text):**
    *   الشعار وبعض الأيقونات تحتاج إلى نصوص بديلة وصفية (مثلاً بدلاً من `alt="Pricing+"` استخدم `alt="أداة تسعير بلس لحساب تكلفة المنتجات"` للصفحة العربية).
4.  **استراتيجية المحتوى (تفعيل قسم "تعلّم التسعير"):**
    *   **أهمية القصوى:** تحويل صفحة `/learn/` إلى مركز أو مدونة للمقالات هو **أقوى خطوة SEO يمكن اتخاذها**. جوجل يحب المواقع التي تقدم قيمة تعليمية مستمرة ومرتبطة بأدواتها.
    *   **طريقة التنفيذ:** إضافة مقالات مبنية على الأسئلة الشائعة لأصحاب المشاريع (Long-tail keywords).
    *   **أمثلة مقترحة للمقالات:**
        *   "كيف تحسب تكلفة منتجاتك اليدوية دون أن تخسر؟ (دليل شامل)"
        *   "ما هي نقطة التعادل ولماذا هي أهم رقم في مشروعك الصغير؟"
        *   "أكبر 5 أخطاء في تسعير الحلويات والكيك المنزلي."
        *   "كيف تسعر منتجك للموزعين مقابل البيع المباشر؟"
    *   **الربط الداخلي (Internal Linking):** في كل مقال، يجب وضع رابط طبيعي يوجه القارئ لاستخدام الأداة (صفحة `/app/`) لتطبيق ما تعلمه، مما يقوي الـ SEO للأداة نفسها.

---

## 5. البيانات المنظمة (Schema Markup / Structured Data)

لمساعدة جوجل على فهم نوع الموقع، يجب إضافة كود (JSON-LD) يوضح أن الموقع هو "أداة ويب" أو "برمجية تطبيق" (SoftwareApplication / WebApplication). 
كما يمكن لاحقاً إضافة (Article Schema) لصفحات المقالات.
هذا قد يجعله يظهر بشكل مميز (Rich Snippets) في نتائج البحث.

*مثال مصغر لما يجب إضافته في `<head>` للصفحة الرئيسية والأداة:*
```html
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": "تسعير+ (Pricing+)",
  "url": "https://pricingplus.app",
  "description": "أداة مجانية لمساعدة المشاريع الصغيرة على حساب السعر الصحيح للمنتجات ونقطة التعادل.",
  "applicationCategory": "BusinessApplication",
  "operatingSystem": "All"
}
</script>
```

---

## 6. خطة العمل التنفيذية (Actionable Checklist)

### 🔴 مهام عاجلة (Quick Wins)
- [ ] تعديل `sitemap.xml` واستبدال الروابط النسبية بروابط مطلقة (`https://pricingplus.app/...`).
- [ ] تحديث رابط الـ sitemap في ملف `robots.txt` ليكون مطلقا.
- [ ] إضافة وسم `<link rel="canonical" href="..." />` لجميع الصفحات كلٌ حسب رابطه.
- [ ] توحيد البيانات الوصفية (العناوين والوصف) في صفحة `index.html` لتتوافق مع لغة واجهة الصفحة الأساسية.

### 🟡 مهام متوسطة المدى (هيكلة واكتساب الزيارات)
- [ ] إضافة كود `Schema.org` (WebApplication) للصفحات الرئيسية.
- [ ] إضافة الكلمات المفتاحية الاستراتيجية العربية داخل نصوص الصفحة بطريقة طبيعية.
- [ ] تحسين نصوص `alt` للصور بحيث تكون موجّهة للـ SEO.
- [ ] **تحويل صفحة `/learn/` إلى هيكل مدونة/مقالات (Blog Layout).**
- [ ] **كتابة ونشر أول 3 مقالات استراتيجية عن التسعير وربطها داخلياً بأداة حساب التسعير.**

### 🟢 مهام استراتيجية (توسعية)
- [ ] التفكير جدياً في فصل رابط النسخة الإنجليزية لتكون صفحات فعلية مستقلة (`/en/`) لتتمكن محركات البحث من أرشفة النسخة الإنجليزية أيضاً، وإضافة وسوم `<link rel="alternate" hreflang="..." />`.
- [ ] إضافة Schema.org من نوع Article لكل مقال جديد يتم نشره.
